#!/usr/bin/env ruby

require 'symphony'
require 'symphony/task'

taskname = ARGV.shift
config   = ARGV.shift || ENV['SYMPHONY_CONFIG']

abort "symphony-task taskname [config.yml]" unless taskname && config

Symphony.load_config( config )
Symphony::Task.get_subclass( taskname ).run

